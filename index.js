/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {Confirmation} from './src/screens/Confirmation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Confirmation);
