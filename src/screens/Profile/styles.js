import {StyleSheet} from 'react-native';
import {colors} from '../../assets';

export default styles = StyleSheet.create({
  container: {
    paddingBottom: 25,
  },
  screenTitle: {
    color: colors.black,
    fontSize: 30,
    marginStart: 24,
  },
  inputStyle: {
    marginTop: 25,
  },
});
